# 更新日志

## 1.0.0 (2026-05-28)

### 新增功能
- 基于 GBZ/T 192 标准的粉尘浓度计算器
- 项目级管理（创建、编辑、复制、删除）
- 样品数据录入（单行 + Excel 批量粘贴）
- 自动计算 W2 平均值、Δm、V0、浓度、修约值
- 四舍六入五成双修约（GB/T 8170）
- 双视图切换：完整视图（编辑）与誊抄视图（只读打印）
- 标准砝码检查记录
- 质控判定：称量质控、增重质控、空白样品质控
- 环境参数变化时自动重算 V0
- 表格键盘导航（Tab/方向键/Enter）

### 技术架构
- 重构：从 Electron 迁移到 Tauri（Rust + rsqlite）
- 体积从 ~150MB 降至 ~4MB
- Apple Silicon 原生应用

### 修复
- 将质控独立列改为内联指示，对齐 Word 模板布局
- 修复检出限临界值修约错误（0.238→0.23 而非 0.24）
- 修复 `batch_create_samples` 无事务问题
- 修复 `get_connection()` unwrap 导致应用崩溃
- 修复 vite 别名 `@` 指向根目录
- 添加 `vite-env.d.ts` 类型声明
- 修复粘贴空值时吞 `0` 的问题
- 修复首次加载时重复 API 调用
- 修复誊抄视图数据同步、高亮、行高问题
- 修复列标题居中和 W₁/W₂ 下标显示
- 多处 catch 块添加错误日志，避免静默吞异常

### 代码质量
- `empty_to_null` 抽取为公共函数，消除重复
- `get_connection()` 返回 `Result` 类型而非直接 unwrap
- `setTimeout(0)` 替换为 `nextTick`
- TypeScript 类型 `any[]` 替换为 `Project[]`
- 去除未使用依赖 `directories`
